import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
    shadow: {
        shadowOffset: {
            width: 2,
            height: 4,
        },
        shadowOpacity: 0.16,
        shadowRadius: 5,
        shadowColor: '#000'
    }
})